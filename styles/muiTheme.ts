import { createTheme } from '@mui/material';

export const journalTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#1D5C63',
    },
    secondary: {
      main: '#2C98A0',
    },
  },
});